// Auth helpers — populated in Step 10 with Supabase email/password flows.
// Until then, treat the app as a single implicit user.
export async function signIn() { throw new Error('Auth not wired yet — see Step 10'); }
export async function signUp() { throw new Error('Auth not wired yet — see Step 10'); }
export async function signOut() { throw new Error('Auth not wired yet — see Step 10'); }
export async function getSession() { return null; }
